{
  "name": "rest-api-testing",
  "version": "1.0.0",
  "description": "In this project I will the testing rest api CRUD end points",
  "main": "./src/server.js",
  "scripts": {
    "start": "node ./src/server.js",
    "test": "cross-env NODE_ENV=test jest --watchAll --detectOpenHandles --testTimeout=10000"
  },
  "author": "Chinedu Emesue",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^26.6.3",
    "mongodb-memory-server": "^6.9.6",
    "supertest": "^6.1.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
